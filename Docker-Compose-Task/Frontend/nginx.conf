resolver 127.0.0.11 valid=10s;

server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/ {
        set $backend http://backend:5000;
        proxy_pass $backend;
    }
}
